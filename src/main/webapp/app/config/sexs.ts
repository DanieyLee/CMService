
export const sexs: any = {
  'true': { name: '男(man)' },
  'false': { name: '女(woman)' },
  // jhipster-needle-i18n-language-key-pipe - JHipster will add/remove languages in this object
};

export const choice = Object.keys(sexs).sort();
